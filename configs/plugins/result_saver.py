
from serie.base.constants import ROOT_PATH


config = {
    "ResultSaver": {
        "output_directory": f"{ROOT_PATH}/outputs",
        "markdown_directory": f"{ROOT_PATH}/../../Notebook/arxiv",
        "keywords_filter_plugin": "",
    }
}
